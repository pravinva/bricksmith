# Bricksmith - Environment Configuration
# Copy this file to .env and fill in your values

# ============================================================================
# REQUIRED: Google AI API Key (for Gemini image generation and analysis)
# ============================================================================
# Get your API key from: https://aistudio.google.com/app/apikey
# 1. Sign in with your Google account
# 2. Click "Get API Key" or "Create API Key"
# 3. Select or create a project
# 4. Copy the key (starts with AIza...)
#
GEMINI_API_KEY=AIza...your-key-here...
GOOGLE_CLOUD_API_KEY=AIza...your-key-here...  # Alternative name (both work)

# ============================================================================
# REQUIRED: Databricks Configuration (for MLflow tracking)
# ============================================================================
# Get your Databricks credentials:
# 1. Go to your Databricks workspace (e.g., https://your-workspace.cloud.databricks.com)
# 2. Click your profile (top right) → Settings
# 3. Go to Developer → Access tokens
# 4. Click "Manage" → "Generate new token"
# 5. Copy the token (starts with dapi...)
# 6. Note your workspace URL and email
#
DATABRICKS_HOST=https://your-workspace.cloud.databricks.com
DATABRICKS_TOKEN=dapi...your-token-here...
DATABRICKS_USER=your.email@company.com
# Do NOT set DATABRICKS_CONFIG_PROFILE here. Databricks auth is one-or-the-other
# (DEFAULT or a named profile like az-field-east). This app uses host+token from
# env; setting a profile can cause "cannot configure default credentials".

# ============================================================================
# OPTIONAL: OpenAI API Key (for gpt-image-1.5 image generation)
# ============================================================================
# Only needed if you use --image-provider openai or set BRICKSMITH_IMAGE_PROVIDER__PROVIDER=openai
# Get your key from: https://platform.openai.com/api-keys
# OPENAI_API_KEY=sk-...your-key-here...

# ============================================================================
# OPTIONAL: GCP Configuration (only if using Vertex AI directly)
# ============================================================================
# Most users don't need this - Google AI Studio (above) is sufficient
GCP_PROJECT_ID=your-gcp-project-id

# ============================================================================
# OPTIONAL: Override Default Settings
# ============================================================================
# Uncomment and modify these if you want to override defaults

# MLflow experiment name (default: /Users/${DATABRICKS_USER}/bricksmith-arch-diagrams)
# BRICKSMITH_MLFLOW__EXPERIMENT_NAME=/Users/your.email@company.com/my-custom-experiment

# Gemini model settings (defaults are optimized for architecture diagrams)
# BRICKSMITH_VERTEX__TEMPERATURE=0.4
# BRICKSMITH_VERTEX__TOP_P=0.95
# BRICKSMITH_VERTEX__MODEL_ID=gemini-3-pro-image-preview

# ============================================================================
# VERIFICATION
# ============================================================================
# After setting up your .env file:
# 1. source .env
# 2. bricksmith check-auth
# You should see:
#   ✓ Google AI API key configured
#   ✓ Databricks credentials configured